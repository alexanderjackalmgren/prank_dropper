# prank_dropper
Drops an audiofile on the computer with autorun functionality, runs once after boot instantly-20min then cleans itself, or you can use the registry version to drop a persistent random playback of the file. The playback is "invisible" unless you know what to look for in Taskmanager.
